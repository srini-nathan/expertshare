import React, { FC } from "react";
import { RouteComponentProps } from "@reach/router";

export const ClientEdit: FC<RouteComponentProps> = (): JSX.Element => {
    return <h1>ClientEdit</h1>;
};

export default ClientEdit;
