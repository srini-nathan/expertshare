import { Theme, makeTheme } from "bootstrap-styled";

const defaultTheme: Theme = makeTheme({
    _name: "default",
    "$brand-primary": "#36989c",
    "$border-radius": "10px",
    "$input-bg": "#ebf5f5",
});
export default defaultTheme;
