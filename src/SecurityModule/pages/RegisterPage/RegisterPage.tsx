import React, { FC } from "react";
import { RouteComponentProps } from "@reach/router";

export const RegisterPage: FC<RouteComponentProps> = (): JSX.Element => {
    return <h1>Register Page</h1>;
};

export default RegisterPage;
